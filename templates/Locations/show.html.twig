{% extends 'base.html.twig' %}

{% block title %}Tbllocations{% endblock %}

{% block body %}
    <h1>Tbllocations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tbllocation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tbllocation.name }}</td>
            </tr>
            <tr>
                <th>Locationtype</th>
                <td>{{ tbllocation.locationtype ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ tbllocation.description }}</td>
            </tr>
            <tr>
                <th>Parentid</th>
                <td>{{ tbllocation.parentid }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tbllocations_index') }}">back to list</a>

    <a href="{{ path('tbllocations_edit', {'id': tbllocation.id}) }}">edit</a>

    {{ include('tbllocations/_delete_form.html.twig') }}
{% endblock %}
